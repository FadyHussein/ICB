# Production Environment Variables for Frontend
# These are baked into the build at build-time by Vite

# Backend API Configuration - Use relative URL in production
VITE_API_BASE_URL=/api/v1

# Application Configuration
VITE_APP_NAME=ICB Sunday School Attendance
VITE_APP_VERSION=1.0.0
